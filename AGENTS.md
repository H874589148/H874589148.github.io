# AGENTS.md — 全局要求

## 删除操作安全规则（Windows）

在 Windows 上进行任何删除操作时，必须遵循以下规则：

- **必须走回收站**：所有文件/目录删除一律使用回收站方式（例如 Python 的 `Send2Trash`，或其他等效的回收站 API/工具），确保内容可恢复。
- **禁止磁盘直接删除命令**：禁用 `del`、`rm`、`rd`、`rmdir`、`erase` 等会绕过回收站、直接从磁盘删除的命令，以及任何等价操作（包括 `Remove-Item` 未加回收站参数、`fs.unlink`、`fs.rm`、`shutil.rmtree` 等直接删除方式）。
- **替代方案**：
  - Python：`Send2Trash`（`send2trash.send2trash(path)`）
  - 其他语言：优先调用系统回收站 API（如 Windows Shell `IFileOperation` 的 `FOF_ALLOWUNDO`，或 `Recycle` 类工具）
- **例外**：仅在用户明确要求"永久删除"时才可绕过回收站，且需先向用户确认。

本条规则适用于本目录及所有下属项目/会话。

## C 盘临时数据写入限制

- **禁止向 C 盘写入临时数据**：不得将临时文件、缓存、中间产物、日志、下载内容等写入 C 盘（包括但不限于 `C:\Users\...\AppData`、`C:\Temp`、`C:\Windows\Temp` 等路径）。
- **临时数据存放位置**：
  - 优先使用当前工作区内的临时目录（如 `_tmp/`），并在任务完成后按删除安全规则清理。
  - 或使用其他非 C 盘盘符下的临时目录。
- **环境变量注意**：执行命令时如需临时目录，显式指定 `TEMP`/`TMP` 到非 C 盘路径，避免工具默认落到 C 盘。
- **例外**：仅用户明确要求写入 C 盘特定路径时才可豁免，且需先向用户确认。

本条规则适用于本目录及所有下属项目/会话。
